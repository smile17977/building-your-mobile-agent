# TODO: ensure your GitHub MCP configuration file (which holds your personal
# access token) is listed here so it is never committed.

# macOS
.DS_Store

# Local Claude Code settings (machine-specific)
.claude/settings.local.json
