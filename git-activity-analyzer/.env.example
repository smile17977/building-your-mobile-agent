# Wave-2 (CI tools) credential. Copy to .env and fill in.
# Read-only scopes are sufficient: actions:read, contents:read, metadata:read.
# Never commit the real .env (see .gitignore).
GITHUB_TOKEN=
# For GitHub Enterprise / GitLab self-hosted, optionally:
# GIT_ACTIVITY_API_BASE=https://api.github.com
